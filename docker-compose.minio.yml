version: '3.8'

services:
  minio:
    image: minio/minio:latest
    container_name: carepoint_minio
    ports:
      - "9000:9000" # S3 API Port
      - "9001:9001" # Web Administration Console Port
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin123
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

volumes:
  minio_data:
